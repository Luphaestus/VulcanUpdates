<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="8dp" /> <!-- Adjust the radius for more or less rounding -->
    <solid android:color="@color/oui_background_color" /> <!-- Background color -->
    <stroke
        android:width="1dp"
        android:color="@color/secondary_text_icon_color" /> <!-- Optional border color -->
</shape>
